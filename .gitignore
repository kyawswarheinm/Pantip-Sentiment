# Environment & secrets
.env
*.env.local

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Local dev database
data/local_dev.db

# Model weights (large binary files — commit to Git LFS or skip)
models/*.pt
models/*.bin
models/*.safetensors

# Exports (regenerated by CI) — keep the Kaggle metadata file
data/exports/*.csv
data/exports/*.zip

# Streamlit — keep config.toml (theme, tracked), ignore secrets only
.streamlit/secrets.toml

# Testing
.pytest_cache/
.coverage
htmlcov/

# IDE
.vscode/
.idea/
*.swp

# macOS
.DS_Store

# Kaggle API credentials (use env vars instead)
kaggle.json
